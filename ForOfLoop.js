const numbers = [123, 456, 345, 47, 87];

for(let i = 0; i < numbers.length; i++)
{
    //console.log(numbers[i]);
}

for (const number of numbers) {
    console.log(number);
}

const myName = "Ashish Saluja";
for(const a of myName) {
    console.log(a);
}